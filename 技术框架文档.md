# 项目技术框架文档

## 📋 项目概述

**项目名称**: comic-picture-book（漫画绘本创作应用）  
**项目描述**: AI驱动的智能漫画绘本创作工具，支持故事脚本生成、自动生成卡通风格漫画绘本、用户认证、作品管理等完整功能  
**开发模式**: SPA（单页应用）模式，客户端渲染  
**版本**: 1.0.0

---

## 🛠️ 核心技术栈

### 前端框架
- **Next.js**: `^16.0.7` - React全栈框架（App Router）
- **React**: `^18.3.1` - UI库
- **React DOM**: `^18.3.1` - DOM渲染

### 开发语言
- **TypeScript**: `^5.0.0` - 类型安全的JavaScript超集
- **配置**: 严格模式，ES2020目标，支持JSX

### 样式方案
- **Tailwind CSS**: `^3.3.0` - 实用优先的CSS框架
- **PostCSS**: `^8.4.0` - CSS后处理器
- **Autoprefixer**: `^10.4.0` - 自动添加浏览器前缀

### HTTP客户端
- **Axios**: `^1.6.0` - Promise基础的HTTP客户端

### 工具库
- **JSZip**: `^3.10.1` - ZIP文件生成和打包工具

### AI服务集成
- **DeepSeek API**: 用于故事脚本生成和对话交互
- **七牛云API** (qnaigc.com): 用于图像生成（卡通风格漫画）

---

## 📁 项目架构

### 目录结构
```
comic-picture-book/
├── app/                          # Next.js App Router
│   ├── api/                      # API路由（服务端）
│   │   ├── script/
│   │   │   └── generate/
│   │   │       └── route.ts      # 脚本生成API
│   │   └── comic/
│   │       ├── generate/
│   │       │   └── route.ts      # 漫画生成API
│   │       ├── save/
│   │       │   └── route.ts      # 图片保存API
│   │       └── cleanup/
│   │           └── route.ts      # 图片清理API
│   ├── layout.tsx                # 根布局（客户端组件）
│   ├── page.tsx                  # 首页（客户端组件）
│   └── globals.css               # 全局样式
│
├── components/                   # React组件
│   ├── Auth/
│   │   └── LoginModal.tsx       # 登录/注册模态框
│   ├── Layout/
│   │   └── MainLayout.tsx        # 主布局（侧边栏导航）
│   ├── ScriptGenerator/         # 脚本生成器组件
│   │   └── ScriptGenerator.tsx
│   ├── ComicGenerator/           # 漫画生成器组件
│   │   └── ComicGenerator.tsx
│   ├── ComicPageCanvas/          # 漫画页面画布组件
│   │   └── ComicPageCanvas.tsx
│   ├── ComicViewer/              # 漫画查看器组件
│   │   └── ComicViewer.tsx
│   ├── ChatInterface/            # 对话交互组件
│   │   └── ChatInterface.tsx
│   └── PersonalCenter/           # 个人中心组件
│       └── PersonalCenter.tsx
│
├── lib/                          # 工具库
│   ├── deepseek.ts              # DeepSeek API客户端
│   ├── qiniu.ts                 # 七牛云API客户端
│   ├── imageGenerator.ts        # 图像生成工具
│   ├── scriptUtils.ts           # 脚本处理工具
│   ├── authUtils.ts             # 用户认证工具
│   ├── downloadUtils.ts         # 下载工具（支持ZIP打包）
│   └── imageStorage.ts          # 图片存储工具
│
├── types/                        # TypeScript类型定义
│   └── index.ts                 # 全局类型定义
│
├── public/                       # 静态资源
│   └── comic-assets/            # 漫画图片存储目录（自动生成）
│
├── scripts/                      # 脚本文件
│   └── get-ip-and-start.js      # 获取IP并启动
│
├── package.json                  # 项目配置和依赖
├── tsconfig.json                 # TypeScript配置
├── next.config.js                # Next.js配置
├── tailwind.config.js            # Tailwind配置
└── postcss.config.js             # PostCSS配置
```

---

## 🔧 技术特性

### 1. Next.js App Router架构
- 使用最新的App Router（Next.js 13+）
- 支持服务端API路由和客户端组件
- 文件系统路由

### 2. SPA模式
- 所有页面组件使用 `'use client'` 指令
- 客户端渲染，提升交互体验
- API路由仍在服务端运行（用于安全处理API密钥）
- 状态持久化（使用sessionStorage）

### 3. TypeScript严格模式
- 完整的类型定义
- 类型安全保证
- 路径别名：`@/*` 映射到项目根目录

### 4. 用户认证系统
- 本地存储的用户认证（localStorage）
- 支持注册、登录、登出
- 用户信息管理（VIP状态、昵称等）

### 5. 图片存储系统
- 自动保存生成的图片到本地存储
- 支持base64和URL两种格式
- 自动清理过期图片（7天过期）
- 定时清理任务（24小时执行一次）

### 6. 下载功能
- 支持单张图片下载
- 支持批量下载（ZIP打包）
- 支持Canvas导出

### 7. 网络访问配置
- 开发环境支持局域网访问（监听 `0.0.0.0`）
- CORS配置支持跨域请求
- Windows防火墙规则自动配置

### 8. 移动端适配
- 响应式布局
- 移动端侧边栏自动收起
- 触摸友好的交互设计

---

## 🔌 API集成

### DeepSeek API
**用途**: 故事脚本生成、对话交互、结构化分镜数据生成

**主要功能**:
- `generateScriptWithDeepSeek()` - 生成文本脚本
- `generateStoryboardWithDeepSeek()` - 生成结构化分镜JSON
- `continueConversation()` - 继续对话完善脚本

**配置**:
- API端点: `https://api.deepseek.com/v1/chat/completions`
- 模型: `deepseek-chat`
- 环境变量: `DEEPSEEK_API_KEY`

### 七牛云API (qnaigc.com)
**用途**: 卡通风格漫画图像生成

**主要功能**:
- 图像生成（同步模式）
- 支持多种参数（negative_prompt, aspect_ratio, human_fidelity等）

**配置**:
- API端点: `https://api.qnaigc.com/v1`
- 模型: `gemini-2.5-flash-image`（当前使用）
- 环境变量: `QINIU_API_KEY`
- 模式: 同步任务模式（直接返回图片URL）

---

## 📡 API路由

### `/api/script/generate`
**方法**: POST  
**功能**: 生成故事脚本或结构化分镜数据

**请求参数**:
```typescript
{
  prompt: string;                    // 用户提示词
  conversationHistory?: Array;       // 对话历史（可选）
  outputFormat?: 'script' | 'storyboard';  // 输出格式
}
```

**响应格式**:
```typescript
{
  success: boolean;
  data: {
    script?: string;                  // 文本脚本（outputFormat='script'）
    storyboard?: StoryboardData;      // 分镜数据（outputFormat='storyboard'）
  };
  error?: string;
}
```

### `/api/comic/generate`
**方法**: POST  
**功能**: 生成漫画绘本页面

**请求参数**:
```typescript
{
  scriptSegment?: string;            // 文本脚本片段（旧格式）
  storyboard?: StoryboardData;        // 分镜数据（新格式，优先）
  startPageNumber?: number;           // 起始页码
}
```

**响应格式**:
```typescript
{
  success: boolean;
  data: {
    pages: ComicPage[];               // 生成的页面数组
  };
  error?: string;
}
```

### `/api/comic/save`
**方法**: POST  
**功能**: 保存图片到本地存储

**请求参数**:
```typescript
{
  imageUrl: string;                   // 图片URL（支持base64）
  pageNumber: number;                 // 页码
  scriptId?: string;                  // 脚本ID（可选）
  segmentId?: number;                 // 片段ID（可选）
}
```

**响应格式**:
```typescript
{
  success: boolean;
  data: {
    url: string;                       // 保存后的本地URL
    fileName: string;                  // 文件名
    expiresAt: string;                // 过期时间（ISO格式）
  };
  error?: string;
}
```

### `/api/comic/cleanup`
**方法**: GET  
**功能**: 清理过期的图片文件

**响应格式**:
```typescript
{
  success: boolean;
  data: {
    deletedCount: number;              // 删除的文件数量
    message: string;                   // 清理结果消息
  };
  error?: string;
}
```

---

## 🎨 核心组件

### MainLayout
- 主布局组件，包含侧边栏导航
- 响应式设计（移动端自动收起）
- 用户认证状态管理
- 页面导航控制

### ScriptGenerator
- 故事脚本生成界面
- 支持对话式交互
- 脚本保存到本地存储

### ComicGenerator
- 漫画绘本生成界面
- 支持脚本导入和选择
- 自动切分长脚本（10页为单位）
- 图片自动保存到本地存储

### ComicPageCanvas
- 漫画页面渲染组件
- 支持对话气泡、旁白显示
- 图像和文本布局
- 支持Canvas导出

### ComicViewer
- 漫画查看器组件
- 支持翻页浏览
- 支持下载功能

### PersonalCenter
- 个人中心组件
- 用户信息管理
- 作品管理

### LoginModal
- 登录/注册模态框
- 用户认证功能

### ChatInterface
- 对话交互组件
- 消息历史管理
- 实时对话反馈

---

## 📦 数据类型

### User（用户）
```typescript
interface User {
  id: string;
  username: string;
  email?: string;
  nickname: string;
  avatar?: string;
  isVip: boolean;
  createdAt: string;
}
```

### Script（脚本）
```typescript
interface Script {
  id: string;
  title: string;
  content: string;
  createdAt: string;
  updatedAt: string;
}
```

### StoryboardData（分镜数据）
```typescript
interface StoryboardData {
  frames: StoryboardFrame[];
}

interface StoryboardFrame {
  frame_id: number;
  image_prompt: string;
  dialogues: DialogueItem[];
  narration?: string;
}

interface DialogueItem {
  role: string;
  text: string;
  anchor: 'left' | 'right' | 'center';
  x_ratio: number;  // 0~1
  y_ratio: number;  // 0~1
}
```

### ComicPage（漫画页面）
```typescript
interface ComicPage {
  pageNumber: number;
  imageUrl: string;
  text: string;
  dialogue?: (string | DialogueItem)[];
  narration?: string;
}
```

### ComicBook（漫画书）
```typescript
interface ComicBook {
  id: string;
  scriptId: string;
  segmentId: number;
  title: string;
  pages: ComicPage[];
  createdAt: string;
  updatedAt?: string;
}
```

---

## 🔧 工具库说明

### authUtils.ts
- `getCurrentUser()` - 获取当前用户
- `saveUser()` - 保存用户信息
- `isLoggedIn()` - 检查登录状态
- `logout()` - 用户登出
- `register()` - 注册新用户
- `login()` - 用户登录

### downloadUtils.ts
- `downloadImageFromUrl()` - 从URL下载图片
- `downloadImageFromCanvas()` - 从Canvas下载图片
- `downloadImagesAsZip()` - 批量下载并打包成ZIP
- `downloadCanvasesAsZip()` - 从Canvas数组打包下载

### imageStorage.ts
- `ensureStorageDir()` - 确保存储目录存在
- `saveImageToStorage()` - 保存图片到本地存储
- `cleanupExpiredImages()` - 清理过期图片

### scriptUtils.ts
- `extractStoryboardFromScript()` - 从脚本提取分镜数据
- `splitScriptIntoSegments()` - 切分脚本为片段
- `loadScriptsFromStorage()` - 从本地存储加载脚本
- `saveScriptToStorage()` - 保存脚本到本地存储
- `loadComicBooksFromStorage()` - 从本地存储加载漫画书
- `saveComicBookToStorage()` - 保存漫画书到本地存储
- `deleteComicBookFromStorage()` - 删除漫画书

---

## ⚙️ 配置文件

### next.config.js
- React严格模式
- 开发环境CORS配置
- 环境变量传递
- 局域网访问支持

### tsconfig.json
- ES2020目标
- 严格类型检查
- 路径别名：`@/*`
- Next.js插件支持

### tailwind.config.js
- 内容路径配置
- 自定义主题色（primary, soft系列）
- 响应式断点

---

## 🔐 环境变量

### 必需变量
- `DEEPSEEK_API_KEY`: DeepSeek API密钥
- `QINIU_API_KEY`: 七牛云API密钥

### 可选变量
- `ENABLE_API_KEY_VERIFICATION`: 是否启用API密钥验证（默认false）

### 配置方式
- 开发环境: `.env.local` 文件
- 生产环境: 服务器环境变量

---

## 🚀 开发与部署

### 开发命令
```bash
npm run dev      # 开发服务器（监听0.0.0.0）
npm run build    # 构建生产版本
npm run start    # 启动生产服务器
npm run lint     # 代码检查
```

### 网络访问
- 开发服务器监听 `0.0.0.0:3000`
- 支持局域网访问
- 自动配置防火墙规则（Windows）

---

## 📝 开发规范

### 代码风格
- TypeScript严格模式
- ESLint配置（Next.js推荐）
- 组件使用函数式组件 + Hooks

### 文件命名
- 组件文件：PascalCase（如 `ComicGenerator.tsx`）
- 工具文件：camelCase（如 `scriptUtils.ts`）
- API路由：小写（如 `route.ts`）

### 组件组织
- 每个组件独立目录
- 组件文件与目录同名
- 相关类型定义在 `types/index.ts`

### 状态管理
- 使用React Hooks（useState, useEffect）
- 本地存储：localStorage（持久化数据）
- 会话存储：sessionStorage（临时状态）

---

## 🔄 数据流

### 脚本生成流程
1. 用户输入提示词 → `ScriptGenerator`
2. 调用 `/api/script/generate` → API路由
3. API调用 `deepseek.ts` → DeepSeek API
4. 返回脚本数据 → 前端显示
5. 用户确认 → 保存到localStorage

### 漫画生成流程
1. 选择/导入脚本 → `ComicGenerator`
2. 脚本切分（如需要）→ `scriptUtils.ts`
3. 调用 `/api/comic/generate` → API路由
4. 解析脚本/分镜 → 生成图像提示词
5. 调用 `qiniu.ts` → 七牛云API生成图像
6. 调用 `/api/comic/save` → 保存图片到本地存储
7. 组装页面数据 → 返回前端
8. `ComicPageCanvas` 渲染 → 显示漫画
9. 保存漫画书到localStorage

### 用户认证流程
1. 用户点击登录 → `LoginModal`
2. 输入用户名密码 → `authUtils.login()`
3. 验证用户信息 → localStorage
4. 保存用户信息 → localStorage
5. 更新UI状态 → `MainLayout`

### 图片清理流程
1. 应用启动 → `layout.tsx` useEffect
2. 调用 `/api/comic/cleanup` → API路由
3. 扫描存储目录 → `imageStorage.ts`
4. 检查过期时间 → 删除过期文件
5. 返回清理结果 → 前端日志

---

## 🎯 技术亮点

1. **AI驱动**: 集成DeepSeek和七牛云AI服务
2. **类型安全**: 完整的TypeScript类型定义
3. **现代化架构**: Next.js 16 App Router
4. **响应式设计**: Tailwind CSS实用优先，支持移动端
5. **SPA体验**: 客户端渲染，流畅交互
6. **用户系统**: 完整的用户认证和作品管理
7. **图片管理**: 自动存储和清理过期图片
8. **下载功能**: 支持单张和批量下载（ZIP打包）
9. **网络友好**: 支持局域网访问，便于团队协作
10. **状态持久化**: 使用localStorage和sessionStorage

---

## 📚 依赖版本

### 生产依赖
- next: ^16.0.7
- react: ^18.3.1
- react-dom: ^18.3.1
- axios: ^1.6.0
- openai: ^4.20.0
- jszip: ^3.10.1

### 开发依赖
- typescript: ^5.0.0
- @types/node: ^20.0.0
- @types/react: ^18.3.0
- @types/react-dom: ^18.3.0
- @types/jszip: ^3.4.1
- tailwindcss: ^3.3.0
- postcss: ^8.4.0
- autoprefixer: ^10.4.0
- eslint: ^9.39.1
- eslint-config-next: ^16.0.7

---

## 🔍 技术栈总结

| 类别 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 框架 | Next.js | 16.0.7 | 全栈React框架 |
| UI库 | React | 18.3.1 | 用户界面 |
| 语言 | TypeScript | 5.0.0 | 类型安全 |
| 样式 | Tailwind CSS | 3.3.0 | 样式框架 |
| HTTP | Axios | 1.6.0 | API调用 |
| 工具 | JSZip | 3.10.1 | ZIP打包 |
| AI服务 | DeepSeek API | - | 脚本生成 |
| AI服务 | 七牛云API | - | 图像生成 |
| 构建 | Next.js Build | - | 构建工具 |
| 代码检查 | ESLint | 9.39.1 | 代码质量 |

---

## 📱 页面视图模式

项目支持以下视图模式（ViewMode）：
- `home` - 首页
- `script` - 脚本生成
- `comic` - 绘本生成
- `edit` - 脚本编辑
- `library` - 作品库
- `ai-create` - AI创作
- `my-works` - 我的作品
- `characters` - 角色管理
- `view-comic` - 查看漫画
- `personal` - 个人中心

---

## 🗂️ 存储结构

### localStorage键
- `comic_scripts` - 保存的脚本列表
- `comic_books` - 保存的漫画书列表
- `current_user` - 当前用户信息
- `is_logged_in` - 登录状态
- `users` - 用户列表
- `user_passwords` - 用户密码（实际应用应加密）

### sessionStorage键
- `currentViewMode` - 当前视图模式（用于状态恢复）

### 文件系统
- `public/comic-assets/` - 漫画图片存储目录
- `public/comic-assets/*.meta.json` - 图片元数据文件

---

*最后更新: 2024年*
